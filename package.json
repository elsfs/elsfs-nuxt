{
  "name": "elsfs-nuxt",
  "private": true,
  "type": "module",
  "scripts": {
    "lint": "pnpm -r --no-bail lint",
    "fmt": "pnpm -r --parallel fmt",
    "fmt:check": "pnpm -r --no-bail fmt:check",
    "typecheck": "pnpm -r --no-bail --if-present typecheck"
  },
  "pnpm": {
    "overrides": {
      "@module-federation/vite": "^1.20.1"
    },
    "onlyBuiltDependencies": [
      "better-sqlite3"
    ]
  },
  "dependencies": {

  }
}
